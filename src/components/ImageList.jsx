const ImageList = () => {
  return (
    <div className="image-list">
      {/* {images.map((image) => (
        <div className="image-card">
          <img src={image.urls.regular} width={100} height={200} />
        </div>
      ))} */}
    </div>
  );
};

export default ImageList;
